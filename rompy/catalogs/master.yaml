description: 'rompy Master Data Catalog'
sources:

  bom:
    args:
      path: "{{CATALOG_DIR}}bom.yaml"
    description: 'Australian Bureau of Meteorology Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
    
  csiro:
    args:
      path: "{{CATALOG_DIR}}csiro.yaml"
    description: 'Commonwealth Scientific and Industrial Research Organisation Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}

  dot_wa:
    args:
      path: "{{CATALOG_DIR}}dot_wa.yaml"
    description: 'WA Department of Transport Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}

  aodn:
    args:
      path: "{{CATALOG_DIR}}aodn.yaml"
    description: 'WA Department of Transport Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}

  oceanum:
    args:
      path: "{{CATALOG_DIR}}oceanum.yaml"
    description: 'Oceanum Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
